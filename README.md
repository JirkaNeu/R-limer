# R-limer
https://github.com/cloudyr/limer

Access LimeSurvey remotly via API and the limer package.

+ + +

The script loads a list of all surveys, adds the number of responses and outputs it as xlsx-file.
